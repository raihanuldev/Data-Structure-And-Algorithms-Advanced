/**
 * Create an function
 * in this function useing for loop
 * ont this for loop create an variable which is hold lowest value
 * then create an nested loop (let j = i+1)
 * check if array[lowest] > arr[j] then set   lowest value => j (index not value)
 * then out of nested loop create an temp variable for swap
 * now swap 
 * // SWAP
        let temp = arr[i];
        arr[i] = arr[lowest]
        arr[lowest] = temp
 * retun arr =>
*/